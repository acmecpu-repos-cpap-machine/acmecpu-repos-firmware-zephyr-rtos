config NTC_THERMISTOR
	bool "NTC Thermistor"
	default y
	depends on DT_HAS_NTC_THERMISTOR_GENERIC_ENABLED || \
		   DT_HAS_MURATA_NXFT15XH103FEAB050_ENABLED      
	select ADC
	help
	  Enable driver for Zephyr NTC Thermistor.
